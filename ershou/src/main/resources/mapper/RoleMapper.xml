<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ershou.ershou.mapper.RoleMapper">

	<select id="getRoles" resultType="com.ershou.ershou.domain.po.Role">
		SELECT sr.*
		FROM sys_role sr
				 JOIN sys_admin_role sur ON sr.role_id = sur.role_id
		WHERE sur.admin_id = #{adminId} AND sr.role_status = 0;
	</select>
</mapper>